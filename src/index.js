import React from 'react';
import ReactDOM from 'react-dom';
import WorkOrderScreen from './App'; // Adjust the import path if necessary

ReactDOM.render(
  <React.StrictMode>
    <WorkOrderScreen />
  </React.StrictMode>,
  document.getElementById('root')
);
